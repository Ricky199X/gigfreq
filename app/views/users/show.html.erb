<header>
   <%= render 'layouts/usernav'%>
</header>

<h1>Welcome, <%=@user.account.username%>!</h1>

<h2>See your upcoming shows below:</h2>
<table class="table">
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

   <tbody>
   <tr>
      <td><strong>Artist</strong></td>
      <td><strong>Venue</strong></td>
      <td><strong>Location</strong></td>
      <td><strong>Date</strong></td>
   </tr>
   
   <% @user.shows.each do |show| %>
      <tr>
         <td><%= show.name%></td>
         <td><%= show.venue%></td>
         <td><%= "#{show.city}, #{show.state}"%></td>
         <td><%= link_to "View Show" , show_path(show)%></td>
      </tr>
   <%end%>
   </tbody>
</table>
