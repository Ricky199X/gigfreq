 <% if current_user.accountable_type == "User" %>
    <nav>
        <div class="nav-wrapper">
            <a href="#" class="brand-logo center">GIGFREQ</a>
                <ul><%= render 'layouts/usernav'%></ul>
        </div>
    </nav>
<% else %>
    <nav>
        <div class="nav-wrapper">
            <a href="#" class="brand-logo center">GIGFREQ</a>
                <ul><%= render 'layouts/bandnav'%></ul>
        </div>
    </nav>
<%end%>